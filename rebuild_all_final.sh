#!/bin/bash
set -e

DOCKER_USER="olegkarenkikh"
IMAGE_NAME="ollama"
LOG_FILE="/tmp/rebuild_all_final.log"

echo "=== ПЕРЕСБОРКА ВСЕХ ФИНАЛЬНЫХ ОБРАЗОВ С ПЕРЕИСПОЛЬЗОВАНИЕМ СЛОЕВ ===" | tee "${LOG_FILE}"
echo "Логирование в: ${LOG_FILE}"
echo "Дата: $(date)" | tee -a "${LOG_FILE}"
echo ""

# Создаем лог-файл с правильными правами
sudo touch "${LOG_FILE}"
sudo chmod 666 "${LOG_FILE}"

# Функция для сборки финального образа с проверкой успешности push
build_final_image() {
    local dockerfile=$1
    local tag=$2
    local build_args=$3
    
    echo "🔨 Сборка финального образа из ${dockerfile}..." | tee -a "${LOG_FILE}"
    local build_output=$(mktemp)
    local build_exit_code=0
    
    if sudo docker buildx build \
        --platform linux/amd64 \
        -f "${dockerfile}" \
        -t "${DOCKER_USER}/${IMAGE_NAME}:${tag}" \
        --push \
        ${build_args} \
        . 2>&1 | tee -a "${LOG_FILE}" "${build_output}"; then
        # Проверяем, что действительно был push
        if grep -qE "pushing|pushed|exporting|exported" "${build_output}" && ! grep -qE "ERROR|failed" "${build_output}"; then
            echo "✅ Финальный образ собран и запушен: ${tag}" | tee -a "${LOG_FILE}"
            rm -f "${build_output}"
            return 0
        else
            echo "❌ Ошибка: образ собран, но push не выполнен или завершился с ошибкой" | tee -a "${LOG_FILE}"
            rm -f "${build_output}"
            return 1
        fi
    else
        build_exit_code=$?
        echo "❌ Ошибка сборки финального образа (код выхода: ${build_exit_code})" | tee -a "${LOG_FILE}"
        rm -f "${build_output}"
        return 1
    fi
}

# ============================================================================
# ВАРИАНТ 1: cuda12amd64 (переиспользует слои)
# ============================================================================
echo "════════════════════════════════════════════════════════════════"
echo "ВАРИАНТ 1: Dockerfile.cuda12amd64-reuse"
echo "════════════════════════════════════════════════════════════════"
build_final_image "Dockerfile.cuda12amd64-reuse" "cuda12amd64" ""
echo ""

# ============================================================================
# ВАРИАНТ 2: minimal (переиспользует слои)
# ============================================================================
echo "════════════════════════════════════════════════════════════════"
echo "ВАРИАНТ 2: Dockerfile.minimal-reuse"
echo "════════════════════════════════════════════════════════════════"
build_final_image "Dockerfile.minimal-reuse" "minimal" ""
echo ""

# ============================================================================
# ВАРИАНТ 3: minimal-v2 (переиспользует слои)
# ============================================================================
echo "════════════════════════════════════════════════════════════════"
echo "ВАРИАНТ 3: Dockerfile.minimal-v2-reuse"
echo "════════════════════════════════════════════════════════════════"
build_final_image "Dockerfile.minimal-v2-reuse" "minimal-v2" ""
echo ""

# ============================================================================
# ВАРИАНТ 4: astra (переиспользует слои)
# ============================================================================
echo "════════════════════════════════════════════════════════════════"
echo "ВАРИАНТ 4: Dockerfile.astra-reuse"
echo "════════════════════════════════════════════════════════════════"
build_final_image "Dockerfile.astra-reuse" "astra" ""
echo ""

echo "════════════════════════════════════════════════════════════════"
echo "Запуск финальной проверки всех образов..." | tee -a "${LOG_FILE}"
echo "════════════════════════════════════════════════════════════════"
echo ""

# Запускаем финальную проверку
bash /workspace/verify_images_final.sh

echo ""
echo "════════════════════════════════════════════════════════════════"
echo "🎉 ПЕРЕСБОРКА ВСЕХ ФИНАЛЬНЫХ ОБРАЗОВ ЗАВЕРШЕНА!"
echo "════════════════════════════════════════════════════════════════"
echo ""
echo "Полный лог сохранен в: ${LOG_FILE}"
