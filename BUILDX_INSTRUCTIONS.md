# Инструкции по сборке контейнеров с Buildx

## ✅ Подготовка завершена

Все необходимые скрипты и документация подготовлены для сборки контейнеров с использованием Docker Buildx.

## 📁 Созданные файлы

### Скрипты сборки:
- `scripts/buildx_build.sh` - Базовая сборка одного Dockerfile
- `scripts/buildx_push.sh` - Создание манифестов и тегов
- `scripts/buildx_all.sh` - Сборка всех вариантов Dockerfile

### Документация:
- `scripts/BUILDX_README.md` - Полная документация
- `scripts/BUILDX_QUICKSTART.md` - Быстрый старт

## 🚀 Быстрый старт

### 1. Проверка окружения
```bash
docker buildx version
```

### 2. Настройка builder (опционально)
```bash
docker buildx create --name ollama-builder --use --bootstrap
```

### 3. Сборка образа

**Локальная сборка (одна платформа):**
```bash
PLATFORM=linux/amd64 ./scripts/buildx_build.sh
```

**Multi-platform сборка с push:**
```bash
PUSH=1 PLATFORM=linux/amd64,linux/arm64 ./scripts/buildx_build.sh
```

**Сборка всех вариантов:**
```bash
PUSH=1 ./scripts/buildx_all.sh
```

## 📋 Доступные Dockerfile варианты

1. `Dockerfile` - Основной (Astra Linux UBI 1.8.1)
2. `Dockerfile.minimal` - Минимальный вариант
3. `Dockerfile.minimal-v2` - Ультра-минимальный вариант
4. `Dockerfile.cuda12amd64` - CUDA 12 AMD64 только

## ⚙️ Основные переменные окружения

- `PLATFORM` - Платформы для сборки (по умолчанию: `linux/amd64,linux/arm64`)
- `VERSION` - Версия образа (автоматически из git)
- `DOCKER_ORG` - Docker organization (по умолчанию: `ollama`)
- `FINAL_IMAGE_REPO` - Полное имя репозитория образа
- `PUSH` - Push в registry (по умолчанию: пусто)
- `DOCKERFILE` - Путь к Dockerfile (по умолчанию: `Dockerfile`)
- `PARALLEL` - Параллельные процессы сборки (по умолчанию: `8`)

## 📖 Подробная документация

См. `scripts/BUILDX_README.md` для полной документации со всеми опциями и примерами.

## ⚠️ Важные замечания

1. **Multi-platform сборка:** Для сборки нескольких платформ одновременно необходимо использовать `PUSH=1` (локальная сборка поддерживает только одну платформу)

2. **Версия:** Версия образа определяется автоматически из git тегов, если не указана через переменную `VERSION`

3. **Builder:** Скрипты автоматически создадут builder, если его нет, или используют существующий

4. **ROCM вариант:** Автоматически собирается для amd64 платформы, если она указана

## 🎯 Готовность к сборке

✅ Все скрипты созданы и имеют права на выполнение  
✅ Документация подготовлена  
✅ Скрипты интегрированы с существующим `env.sh`  
✅ Поддержка всех вариантов Dockerfile  
✅ Поддержка multi-platform сборки  
✅ Поддержка push в registry  

**Система готова к получению инструкций по сборке!**
